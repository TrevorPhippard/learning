import { Module } from "@nestjs/common";

@Module({
  imports: [], // add feature modules here later
  controllers: [], // add controllers here later
  providers: [], // add services/providers here later
})
export class AppModule {}
